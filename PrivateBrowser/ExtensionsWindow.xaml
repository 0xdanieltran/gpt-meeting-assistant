<Window x:Class="PrivateBrowser.ExtensionsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Extensions"
        Width="560"
        Height="480"
        MinWidth="480"
        MinHeight="400"
        WindowStartupLocation="CenterOwner"
        Topmost="True"
        ResizeMode="CanResize">

    <Grid Margin="20">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock
            Grid.Row="0"
            Text="Browser extensions"
            FontSize="20"
            FontWeight="SemiBold"
            Margin="0,0,0,8"/>

        <StackPanel
            Grid.Row="1"
            Margin="0,0,0,14">

            <TextBlock
                TextWrapping="Wrap"
                Margin="0,0,0,6"
                Foreground="#555555"
                Text="WebView2 cannot install from the Chrome Web Store page itself. Paste a store link or extension ID to download and sideload it, or load an unpacked folder that contains manifest.json."/>

            <TextBlock
                x:Name="InstallPathText"
                TextWrapping="Wrap"
                FontSize="11"
                Foreground="#777777"/>

        </StackPanel>

        <Grid Grid.Row="2" Margin="0,0,0,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBox
                Grid.Column="0"
                x:Name="StoreInput"
                Height="32"
                Padding="8,0"
                VerticalContentAlignment="Center"
                KeyDown="StoreInput_KeyDown"/>

            <Button
                Grid.Column="1"
                x:Name="InstallButton"
                Content="Install"
                Width="90"
                Height="32"
                Margin="8,0,0,0"
                Click="InstallButton_Click"/>
        </Grid>

        <ListBox
            Grid.Row="3"
            x:Name="ExtensionsList"
            Margin="0,0,0,12"
            MouseDoubleClick="ExtensionsList_MouseDoubleClick"/>

        <StackPanel
            Grid.Row="4"
            Orientation="Horizontal"
            HorizontalAlignment="Right"
            Margin="0,0,0,10">

            <Button
                x:Name="LoadUnpackedButton"
                Content="Load unpacked"
                Height="32"
                Padding="12,4"
                Margin="0,0,8,0"
                Click="LoadUnpackedButton_Click"/>

            <Button
                x:Name="OpenButton"
                Content="Open"
                Height="32"
                Padding="12,4"
                Margin="0,0,8,0"
                Click="OpenButton_Click"/>

            <Button
                x:Name="RemoveButton"
                Content="Remove"
                Height="32"
                Padding="12,4"
                Click="RemoveButton_Click"/>
        </StackPanel>

        <TextBlock
            Grid.Row="5"
            x:Name="StatusText"
            TextWrapping="Wrap"
            Foreground="#555555"/>

    </Grid>
</Window>
